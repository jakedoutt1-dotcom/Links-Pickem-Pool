const json=(data,status=200)=>Response.json(data,{status,headers:{"Cache-Control":"no-store"}});
async function ensureSchema(db){await db.batch([db.prepare("CREATE TABLE IF NOT EXISTS pools(id TEXT PRIMARY KEY,code TEXT UNIQUE NOT NULL,name TEXT NOT NULL,commissioner_email TEXT NOT NULL,phone TEXT,active INTEGER DEFAULT 1,created_at TEXT NOT NULL)"),db.prepare("CREATE TABLE IF NOT EXISTS pool_games(pool_id TEXT NOT NULL,game TEXT NOT NULL,PRIMARY KEY(pool_id,game))"),db.prepare("CREATE TABLE IF NOT EXISTS players(id TEXT PRIMARY KEY,email TEXT UNIQUE NOT NULL,display_name TEXT,created_at TEXT NOT NULL)"),db.prepare("CREATE TABLE IF NOT EXISTS memberships(pool_id TEXT NOT NULL,player_id TEXT NOT NULL,role TEXT DEFAULT 'player',status TEXT DEFAULT 'active',joined_at TEXT NOT NULL,PRIMARY KEY(pool_id,player_id))")])}
const gameLabel={nfl:"NFL Pick’em",college:"College Pick’em",survivor:"Survivor",confidence:"Confidence","33":"Game 33",squares:"Squares",march:"March Madness",masters:"Golf",nascar:"NASCAR",fantasy:"Fantasy",dynasty:"Dynasty",custom:"Custom"};
async function getNew(db,id,code){if(!db)return null;await ensureSchema(db);let p=null;if(id)p=await db.prepare('SELECT id,code,name,active,created_at AS createdAt FROM pools WHERE id=? LIMIT 1').bind(String(id)).first();if(!p&&code)p=await db.prepare('SELECT id,code,name,active,created_at AS createdAt FROM pools WHERE upper(code)=upper(?) LIMIT 1').bind(code).first();if(!p)return null;const g=await db.prepare('SELECT game FROM pool_games WHERE pool_id=? ORDER BY game').bind(p.id).all();return {...p,games:(g.results||[]).map(x=>x.game),game:g.results?.[0]?.game||'',role:'player',dataSource:'LINKS_DB'}}
async function getOld(db,id,code){if(!db)return null;let p=null;if(id&&/^\d+$/.test(String(id)))p=await db.prepare('SELECT id,code,name,created_at AS createdAt FROM pools WHERE id=? LIMIT 1').bind(Number(id)).first();if(!p&&code)p=await db.prepare('SELECT id,code,name,created_at AS createdAt FROM pools WHERE upper(code)=upper(?) LIMIT 1').bind(code).first();if(!p)return null;let games=[];try{const r=await db.prepare('SELECT game_type FROM pool_active_games WHERE pool_id=? AND active=1 ORDER BY is_primary DESC,game_type').bind(p.id).all();games=(r.results||[]).map(x=>gameLabel[String(x.game_type||'').toLowerCase()]||x.game_type)}catch{}if(!games.length)games=['NFL Pick’em'];return {...p,id:String(p.id),active:1,games,game:games[0],role:'player',dataSource:'legacy-production-DB'}}
export async function onRequestGet({request,env}){const q=new URL(request.url).searchParams,id=String(q.get('id')||'').trim(),code=String(q.get('code')||'').trim(),search=String(q.get('search')||'').trim();if(search){let pools=[];if(env.LINKS_DB)try{await ensureSchema(env.LINKS_DB);const like='%'+search.toLowerCase()+'%',r=await env.LINKS_DB.prepare('SELECT id,code,name,active,created_at AS createdAt FROM pools WHERE active=1 AND (lower(name) LIKE ? OR lower(code) LIKE ?) ORDER BY name LIMIT 12').bind(like,like).all();for(const p of r.results||[]){const x=await getNew(env.LINKS_DB,p.id,p.code);if(x)pools.push(x)}}catch{}if(env.DB)try{const r=await env.DB.prepare('SELECT id,code,name,created_at AS createdAt FROM pools WHERE lower(name) LIKE ? OR upper(code) LIKE ? ORDER BY lower(name) LIMIT 12').bind('%'+search.toLowerCase()+'%','%'+search.toUpperCase()+'%').all();for(const p of r.results||[]){const x=await getOld(env.DB,p.id,p.code);if(x&&!pools.some(n=>String(n.code).toUpperCase()===String(x.code).toUpperCase()))pools.push(x)}}catch{}return json({success:true,pools:pools.slice(0,12),build:'690'})}if(!id&&!code)return json({success:false,error:'Pool code, id or search required'},400);let pool=null;try{pool=await getNew(env.LINKS_DB,id,code)}catch{}if(!pool)try{pool=await getOld(env.DB,id,code)}catch{}return pool?json({success:true,pool,build:'690'}):json({success:false,error:'Pool not found'},404)}
export async function onRequestPost({request,env}){const db=env.LINKS_DB;if(!db)return json({success:false,error:'LINKS database is not available'},503);await ensureSchema(db);let b;try{b=await request.json()}catch{return json({success:false,error:'Invalid request'},400)}if(b.action!=='createPool')return json({success:false,error:'Unsupported action'},400);const name=String(b.name||'').trim(),email=String(b.email||'').trim().toLowerCase(),phone=String(b.phone||'').trim(),games=[...new Set((Array.isArray(b.games)?b.games:[]).map(String).map(x=>x.trim()).filter(Boolean))];if(name.length<3||!email.includes('@')||!games.length)return json({success:false,error:'Pool name, commissioner email and at least one game are required'},400);const id=crypto.randomUUID(),playerId=crypto.randomUUID(),now=new Date().toISOString(),code=(name.replace(/[^a-z0-9]/gi,'').slice(0,4).toUpperCase()||'LINK')+'-'+crypto.randomUUID().replaceAll('-','').slice(0,5).toUpperCase();try{await db.batch([db.prepare('INSERT INTO pools(id,code,name,commissioner_email,phone,active,created_at) VALUES(?,?,?,?,?,1,?)').bind(id,code,name,email,phone,now),db.prepare('INSERT OR IGNORE INTO players(id,email,display_name,created_at) VALUES(?,?,?,?)').bind(playerId,email,name+' Commissioner',now),db.prepare("INSERT INTO memberships(pool_id,player_id,role,status,joined_at) VALUES(?,?,'commissioner','active',?)").bind(id,playerId,now),...games.map(g=>db.prepare('INSERT INTO pool_games(pool_id,game) VALUES(?,?)').bind(id,g))])}catch(e){return json({success:false,error:'Pool could not be created.',detail:String(e?.message||e)},409)}return json({success:true,pool:{id,code,name,commissionerEmail:email,phone,games,game:games[0],role:'commissioner',playerId},build:'690'})}